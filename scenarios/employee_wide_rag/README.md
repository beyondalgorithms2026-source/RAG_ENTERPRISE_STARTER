# Scenario Pack: employee_wide_rag

Internal employee assistant where every authenticated employee has equal access to enabled corpora.

## Build Choices
- `AUTH_MODE=dev` locally; replace with `oidc` for real deployment.
- `ACCESS_STRATEGY=employee_all`
- Source/corpus administration and observability remain enabled.
- Advanced enterprise governance, tuning, connectors, and actions are disabled by default.
